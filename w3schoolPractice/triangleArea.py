# Write a Python program that will accept the base and height of a triangle and compute the area.
def area_calculator(b, h):
    # b = int(input("Please enter a base for our triangle"))
    # h = int(input("Please enter a height for our triangle"))
    print((b // 2) * h)

area_calculator(20, 40)